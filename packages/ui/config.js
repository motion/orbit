// so you can require this before requiring rest of UI kit, so config can be more static

exports.configureUI = require('./_/helpers/configure')
exports.configureHotKeys = require('./_/helpers/configureHotKeys')
