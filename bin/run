#!/bin/bash

. $(dirname $0)/__/common
cd $ROOT

trap killgroup SIGINT
killgroup() {
  kill 0
}

cd app/$1

if [ "$2" = "--prod" ]; then
  npm run start-prod -- ${@:3}
else
  npm run start -- ${@:2}
fi
