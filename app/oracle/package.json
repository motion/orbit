{
  "name": "@mcro/oracle",
  "version": "2.3.10",
  "private": true,
  "main": "./_/Oracle.js",
  "scripts": {
    "install-pods": "pod install",
    "build": "./build.sh --if-empty && npx tsc || true",
    "watch": "tsc --watch --preserveWatchOutput"
  },
  "dependencies": {
    "@mcro/logger": "^2.3.10",
    "electron-util": "0.9.0",
    "fs-extra": "7.0.0",
    "kill-port": "1.3.1"
  }
}
