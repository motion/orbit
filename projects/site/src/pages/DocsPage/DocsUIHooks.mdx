<IntroText>
  The UI Kit exports all the hooks it uses internally, so you can share much of the logic.
</IntroText>

The one big upside of the hooks in the UI kit is that they all work together, using similar APIs.

## Positioning

<Example id="Basic" onlySource chromeless />
