import * as React from 'react'
import { Grid } from '@mcro/ui'

export const HomePage = () => {
  return <Grid>hi</Grid>
}

HomePage.path = ''
HomePage.navigationOptions = {
  title: 'Home',
  linkName: 'Home Page',
}
