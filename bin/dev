#!/bin/bash

. $(dirname $0)/__common
cd $ROOT

# set dev context
export KUBECONFIG=infra/keys/kubes/dev
kubectl config use-context minikube

# minikube stuff
eval $(minikube docker-env)

bin/__kubes ${@:1}
