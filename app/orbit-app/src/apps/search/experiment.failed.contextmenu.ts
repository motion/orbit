// const contextMenu = React.useRef<ContextMenuHandler>(null)
{
  /* <View
                onClick={flow(
                  preventDefault,
                  () => {
                    console.log('trying to show......', contextMenu.current)
                    contextMenu.current.show({
                      direct: true,
                      // x: 100,
                      // y: 100,
                    })
                  },
                )}
              >
                <ContextMenu
                  items={[
                    {
                      label: 'test',
                    },
                  ]}
                  ref={contextMenu}
                >
                  <View
                    alignItems="center"
                    justifyContent="center"
                    width={34}
                    opacity={0.5}
                    hoverStyle={{ opacity: 1 }}
                  >
                    <Icon name="dots" size={12} />
                  </View>
                </ContextMenu>
              </View> */
}
