The `<Progress />` component support `type="circular"` or `type="bar"`, and defaults to bar.

To use a Progress:

```tsx
import { Progress } from '@o/ui'
```

<br />

## Examples

### Basics

<Example id="Simple" />
