import { gloss } from '@mcro/gloss'

export const Centered = gloss({
  position: 'absolute',
  top: 0,
  left: 150,
  right: 150,
  bottom: 0,
  overflow: 'hidden',
  alignItems: 'center',
  justifyContent: 'center',
  textAlign: 'center',
})
