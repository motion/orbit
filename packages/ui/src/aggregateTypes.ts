// for use with our documentation generation
// this documents the types we want to aggregate visually
// so we don't clutter parent types

export type ButtonElementProps = React.HTMLAttributes<HTMLButtonElement>
