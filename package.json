{
  "name": "motion-starter",
  "private": true,
  "scripts": {
    "bootstrap": "npm run check-dependencies && lerna bootstrap --npm-client=yarn --ignore starter-native",
    "web": "npm run check-dependencies && cd apps/web && npm start",
    "web-debug": "cd apps/web && npm run start-debug",
    "clean": "rm -rf apps/*/lib && rm -rf apps/*/node_modules",
    "build": "lerna exec --scope api --scope @jot/models -- babel src --out-dir lib -s true",
    "build-web": "cd apps/web && npm run build",
    "compile": "npm run clean && npm run build",
    "watch": "npm run build -- --watch",
    "api": "COUCHDB_USER=admin COUCHDB_PASSWORD=password docker-compose up --remove-orphans",
    "electron": "cd apps/web && npm run electron",
    "check-dependencies": "lerna exec --ignore starter-native -- check-dependencies"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-eslint": "^7.2.3",
    "babel-plugin-root-import": "*",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",
    "babel-preset-env": "^1.1.8",
    "babel-preset-react": "^6.11.1",
    "babel-preset-stage-0": "^6.5.0",
    "check-dependencies": "^1.0.1",
    "eslint": "^3.19.0",
    "eslint-import-resolver-babel-root-import": "0.0.2",
    "eslint-plugin-class-property": "^1.0.6",
    "eslint-plugin-react": "^7.0.1",
    "flow-bin": "^0.38.0",
    "lerna": "2.0.0-rc.3"
  }
}
