{
  "name": "@mcro/screen",
  "version": "4.0.0",
  "license": "MIT",
  "scripts": {
    "test": "node --experimental-modules ./test --debug && open ./tmp",
    "test-fast": "node --experimental-modules ./test",
    "build": "cd swift && xcodebuild archive -derivedDataPath $(mktemp -d) -scheme aperture",
    "prepublish": "npm run build"
  },
  "files": [
    "index.js",
    "aperture"
  ],
  "dependencies": {
    "reconnecting-websocket": "^3.2.2",
    "electron-util": "^0.2.1",
    "execa": "^0.8.0",
    "macos-version": "^4.0.1"
  }
}
