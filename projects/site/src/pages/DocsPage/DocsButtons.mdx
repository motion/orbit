import * as Ex from './DocsButtons'
import ExSource from '!raw-loader!./DocsButtons'
import { Example } from './Example'

A `<Button />` is a simple extension of a `<Surface />`.

## Examples

### Basics

<Example examples={Ex} source={ExSource} id="One" />

### Alternates

<Example examples={Ex} source={ExSource} id="Two" />

### Sizing (Preset)

<Example examples={Ex} source={ExSource} id="Three" />

### Elevation + Sizing (Absolute)

<Example examples={Ex} source={ExSource} id="Four" />
