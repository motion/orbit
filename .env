# paths
export PATH=`pwd`/bin:$PATH
export ROOT=$PWD

# nginx
export HOST_IP=$(ipconfig getifaddr en0 || ipconfig getifaddr en1)

# kubernetes
export NAMESPACE=starter
export DOCKER_SECRET=docker-hub
export DOCKER_REGISTRY_SERVER=https://index.docker.io/v1/
export DOCKER_USER=nwienert
export DOCKER_EMAIL=natewienert@gmail.com
export DOCKER_PASSWORD=fluf499

export KUBECONFIG=kubeconfig

# couch
export DB_HOST=pad-couch
export DB_PORT=5984
export DB_PROTOCOL=http://

# api

# web
export PUBLIC_URL=jot.dev

export DOCKER_OPTS="--insecure-registry localhost:5000"
