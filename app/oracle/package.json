{
  "name": "@mcro/oracle",
  "version": "2.3.0",
  "private": true,
  "main": "./_/Oracle.js",
  "scripts": {
    "build": "./build.sh --if-empty && npx tsc || true",
    "watch": "tsc --watch --preserveWatchOutput"
  },
  "dependencies": {
    "@mcro/logger": "^2.3.0",
    "electron-util": "0.9.0",
    "fs-extra": "7.0.0",
    "kill-port": "1.3.1"
  },
  "publishConfig": {
    "registry": "http://localhost:4343"
  }
}
