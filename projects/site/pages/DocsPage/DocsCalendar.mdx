The `<Calendar />` in Orbit is based on react-date-range, because it is one of the only calendars
that allows click-and-drag selections of date ranges.

To use a Calendar:

```tsx
import { Calendar } from '@o/ui'
```

## Examples

### Basics

<Example id="Basic" />
