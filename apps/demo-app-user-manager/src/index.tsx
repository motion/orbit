import { createApp } from '@o/kit'

import { DemoAppUserManager } from './App'

export default createApp({
  id: 'demo-app-user-manager',
  name: 'App Demo: User Manager',
  icon: '',
  app: DemoAppUserManager,
})
