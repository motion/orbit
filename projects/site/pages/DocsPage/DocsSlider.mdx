The `<Slider />` component uses `<Interactive />` to create a visually appealing slider which you
can resize.

To use a Slider:

```tsx
import { Slider } from '@o/ui'
```

## Examples

### Basics

<Example id="Basic" />
