apiVersion: v1
kind: Service
metadata:
  name: pad-web
  labels:
    run: pad-web
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31000
      name: http
  selector:
    run: pad-web
status:
  loadBalancer: {}
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: pad-web
spec:
  replicas: 1
  strategy: {}
  template:
    metadata:
      labels:
        run: pad-web
    spec:
      imagePullSecrets:
        - name: docker-hub
      containers:
      - name: pad-web
        image: index.docker.io/motion/starter-web:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources: {}
status: {}

