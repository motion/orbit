import * as React from 'react'
import { Button } from '@mcro/ui'

export const TitleBarButton = props => (
  <Button size={0.9} sizeRadius={1} sizeFont={1.1} sizePadding={0.95} iconSize={12} {...props} />
)
