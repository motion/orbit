import * as React from 'react'
import { RoundButton } from './RoundButton'

export const RoundButtonSmall = props => (
  <RoundButton sizeHeight={0.65} sizePadding={0.7} size={0.95} fontWeight={400} {...props} />
)
