The `<Tag />` component is a simple extension of the `<SizedSurface />`, which adjusts some sizing
and spacing to look nice for use as a label.

To use a Tag:

```tsx
import { Tag } from '@o/ui'
```

## Examples

### Basics

Tags are well-used with the light variants of themes.

<Example id="Basic" />
