import { view } from '@mcro/black'

export const PeekActionBar = view({
  padding: [4, 5, 5],
  flexFlow: 'row',
  overflow: 'hidden',
  alignItems: 'center',
})

PeekActionBar.Space = view({
  width: 10,
})
