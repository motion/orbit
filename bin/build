#!/bin/bash

. $(dirname $0)/__/common

APP=$1
ARGS=${@:1}

if [ "$APP" = "electron" ]; then
  cd $ROOT/apps/electron
  npm run dist
elif [ "$APP" = "web" ]; then
  cd $ROOT/apps/web
  npm run build
else
  # ensure linked in
  yarn link @jot/preset-motion
  # lerna
  lerna exec --parallel \
    --ignore @jot/native  \
    --ignore @jot/web \
    --ignore @jot/couch \
    --ignore playground \
    --ignore @jot/preset-motion \
      -- \
      babel src --out-dir lib -s true $ARGS
fi
