The `<Sidebar />` component uses `<Interactive />` to create a visually appealing sidebar which you
can resize.

To use a Sidebar:

```tsx
import { Sidebar } from '@o/ui'
```

## Examples

### Basics

<Example id="Basic" />
