{
  "name": "@mcro/swindler",
  "version": "4.0.0",
  "license": "MIT",
  "scripts": {
    "test": "node --experimental-modules ./test",
    "build": "cd Swindler && xcodebuild archive -derivedDataPath $(mktemp -d) -scheme SwindlerExample -configuration Release -workspace Swindler.xcworkspace"
  },
  "files": [
    "index.js",
    "aperture"
  ],
  "dependencies": {
    "electron-util": "^0.2.1",
    "execa": "^0.8.0"
  }
}
