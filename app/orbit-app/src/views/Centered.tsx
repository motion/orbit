import { view } from '@mcro/black'

export const Centered = view({
  position: 'absolute',
  top: 0,
  left: 150,
  right: 150,
  bottom: 0,
  overflow: 'hidden',
  alignItems: 'center',
  justifyContent: 'center',
  textAlign: 'center',
})
