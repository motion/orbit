#!/bin/bash

. $(dirname $0)/__common
cd $ROOT

# ensure docker
# ensure kubernetes
# ensure minikube
# ensure xhyve
# ensure npm
# npm bootstrap
# ensure /etc/hosts to have 127.0.0.1 jot.dev
# run minikube
