import { view } from '@mcro/black'

export const MarkdownBody = view({
  width: '100%',
  flex: 1,
  overflowX: 'hidden',
  overflowY: 'scroll',
  padding: 22,
  fontSize: 16,
  lineHeight: 24,
  display: 'block',
})
