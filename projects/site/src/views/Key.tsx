import { Button, gloss } from '@o/ui'

export const Key = gloss(Button, {
  display: 'inline',
  size: 'xs',
  glint: false,
  fontWeight: 600,
  // background: 'red', //theme => theme.background,
  hoverStyle: null,
  activeStyle: null,
  noInnerElement: true,
})
