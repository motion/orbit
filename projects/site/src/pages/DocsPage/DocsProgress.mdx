The `<Progress />` component supports `type="circular"` or `type="bar"`, it defaults to a bar.

To use a Progress:

```tsx
import { Progress } from '@o/ui'
```

<br />

## Examples

### Basics

<Example id="Simple" />
