#!/bin/bash

ARGS=${@:1}

echo "install: $ARGS"

if [ -z "$ARGS" ]; then
  npx oao bootstrap
else
  NAME=$(grep '"name":' package.json | cut -d\" -f4)
  echo "oao add $NAME $ARGS"
  npx oao add $NAME $ARGS
fi

