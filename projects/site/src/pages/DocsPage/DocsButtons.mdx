import * as Ex from './DocsButtons'
import ExSource from '!raw-loader!./DocsButtons'
import { Example } from './Example'

A `<Button />` is a simple extension of a `<Surface />`.

```tsx
import { Button } from '@o/ui'
```

## Examples

### Basics

<Example examples={Ex} source={ExSource} id="Basic" />

### Alternates

<Example examples={Ex} source={ExSource} id="Alternates" />

### Sizing (Preset)

<Example examples={Ex} source={ExSource} id="Sizing" />

### Grouping

<Example examples={Ex} source={ExSource} id="Group" />

### Elevation + Sizing (Absolute)

<Example examples={Ex} source={ExSource} id="Elevation" />
