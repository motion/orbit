import CardSrc from '!raw-loader!@o/ui/src/Card.tsx'
import Description from '../../../tmp/Button.json'
import * as Ex from './DocsCards'
import { Example } from './Example'

A `<Card />` combines a `<SizedSurface />` with a `<ListItem />` as it's header.
It's `<Collapsable />` as well.

## `<Card />`

### Props, Sizing, and Theme

<Example>{Ex.Basics}</Example>

### Collapsable

<Example>{Ex.Collapsable}</Example>

## `<FloatingCard />` <small>(BETA)</small>

A `<FloatingCard />` allows you to place a card somewhere floating in the interface.

<Example>
  <Ex.Floating />
</Example>
