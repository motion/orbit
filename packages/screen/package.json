{
  "name": "@mcro/screen",
  "private": true,
  "version": "4.0.0",
  "license": "MIT",
  "main": "es6/index",
  "scripts": {
    "test": "node ./test --debug && open ./tmp",
    "test-fast": "node ./test",
    "build": "cd swift && xcodebuild archive -derivedDataPath $(mktemp -d) -scheme aperture",
    "prepublish": "npm run build"
  },
  "files": [
    "index.js",
    "aperture"
  ],
  "dependencies": {
    "reconnecting-websocket": "^3.2.2",
    "html5-websocket": "^2.0.2",
    "electron-util": "^0.2.1",
    "execa": "^0.8.0",
    "macos-version": "^4.0.1",
    "ws": "^4.0.0"
  }
}
