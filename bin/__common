#!/bin/bash

APP_OLD_PWD="$(pwd)"
OLD_PATH=$PATH

# cd ROOT
cd $(dirname $0)/..

# source env
source .env

# set path/root
PATH=/usr/local/bin:/usr/bin
ROOT="$(pwd)"

# cd back to original dir
PATH=$OLD_PATH:$ROOT/node_modules/.bin
cd $APP_OLD_PWD
